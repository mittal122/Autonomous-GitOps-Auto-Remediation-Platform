# gitops/

ArgoCD Application definitions that implement the GitOps sync loop.
ArgoCD watches this directory and reconciles the cluster state to match.

**Status:** Empty skeleton — ArgoCD app manifests are a future prompt.

<!-- TODO (future prompt): add ArgoCD Application CRDs per service -->
