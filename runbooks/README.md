# runbooks/

Machine-readable remediation action definitions.
Each runbook describes a specific corrective action (e.g., memory-limit-increase, rollback-deployment)
in a structured format that the remediator can execute.

**Status:** Empty skeleton — runbook definitions are a future prompt.

<!-- TODO (future prompt): add YAML runbook schemas per action type -->
